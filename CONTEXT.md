# 项目领域词汇表

## 系统层级

- **煤矿**：矿山企业，用 `mine_code`（煤矿编码）唯一标识
- **监控系统**：煤矿下的六大子系统，每个有唯一 `system_code`
- **设备**：某个监控系统下的物理设备（如"1号主通风机"），有 `device_name` 和 `device_code`
- **PLC设备**：可编程逻辑控制器，用 `DeviceConfig` 表示，提供连接信息（host/port/协议）

## 六大系统

| 简称 | 系统名称 | system_code |
|------|----------|-------------|
| tfjk | 主要通风机监控系统 | 30 |
| psjk | 主排水监控系统 | 31 |
| lijk | 立井提升监控系统 | 32 |
| xjjk | 斜井提升监控系统 | 33 |
| kyjk | 空气压缩机监控系统 | 34 |
| jcjk | 绞车监控系统 | 35 |

## 数据类型

- **静态数据**：基础台账信息，不随时间变化（JBSJ/ABSJ/JZTT/JCYJ）
- **动态数据**：PLC 实时采集数据，分三类：
  - **JC（测点基础）**：寄存器定义信息
  - **SS（实时数据）**：当前采集值
  - **YC（异常数据）**：报警/故障记录

## 寄存器与采集

- **寄存器测点（RegisterPoint）**：PLC 中的一个数据点，有地址、数据类型、量程等属性
- **采集启用（collect_enabled）**：开启后寄存器纳入周期采集队列
- **报文参与（report_enabled）**：控制该寄存器数据是否参与报文生成
- **故障缺省值（fault_default）**：采集失败时的填充值，默认 -9999

## 报文

- **报文**：按 MT/T 1201.2-2023 标准格式化的数据文件
- **格式规范**：字段分隔 `;`，单条结尾 `~`，文件结尾 `||`，UTF-8 无 BOM
- **文件名**：由可配置字段拼接，默认 `煤矿编码_类型_时间戳.txt`

## 上传

- **FTP上传**：将 `data/` 目录下的文件推送到远程 FTP 服务器
- **MQ发布**：将报文发布到消息队列（RabbitMQ 或本地日志）
- **文件筛选（file_filter）**：FTP 上传时按文件类型标识过滤
