# 上传配置功能优化需求
## 一、整体说明
在原有【标准报文管理】页面新增【上传配置】按钮，实现报文项目自定义勾选、定时任务配置、六大系统测点寄存器选配、寄存器新增/编辑/存储/页面展示；**新增寄存器信息只做台账保存，不默认修改原有采集任务，按需勾选采集标识才接入采集，互不影响现有采集逻辑**，最终按照MT/T1201.2-2023规范自动生成报文。

## 二、功能入口
按钮名称：`上传配置`
摆放位置：标准报文管理页面固定功能按钮，点击弹出配置弹窗。

## 三、通用配置区：生成项目+定时设置
### 3.1 待生成数据项目（多选框，支持全选/反选）
#### 3.1.1 静态基础数据
- JBSJ：矿用设备基本信息
- ABSJ：矿用设备安标信息
- JZTT：禁止/淘汰设备信息
- JCJY：矿用设备检测检验信息

#### 3.1.2 六大设备动态数据（单个系统独立勾选：JC基础/SS实时/YC异常三类）
|系统简称|系统名称|可选数据类型|
| ---- | ---- | ---- |
|tfjk|主要通风机|JC测点基础、SS实时、YC异常|
|psjk|主排水|JC测点基础、SS实时、YC异常|
|lijk|立井提升|JC测点基础、SS实时、YC异常|
|xjjk|斜井提升|JC测点基础、SS实时、YC异常|
|kyjk|空压机|JC测点基础、SS实时、YC异常|
|jcjk|绞车|JC测点基础、SS实时、YC异常|

### 3.2 定时任务配置（选择控件）
1. 定时类型单选：自定义分钟、自定义小时、每日固定时点、每周指定日期+时点
2. 时间选择框：根据选中类型填入对应执行时间
3. 任务启用开关：启用/停用当前定时配置
> 规则：勾选的数据项目绑定对应定时方案，到达设定时间自动生成报文。

## 四、六大动态系统专项：测点寄存器配置
> 动态报文数据源：PLC寄存器实时采集数据；每个系统右侧配置【数据选择】按钮，点击打开测点配置弹窗。
### 4.1 弹窗页面内容
#### 4.1.1 已存寄存器测点列表（表格展示字段）
测点编码、测点名称、寄存器地址、数据类型、量程上限、量程下限、报警上限、报警下限、计量单位、采集启用状态、勾选框（是否参与报文生成）

#### 4.1.2 新增寄存器功能按钮
点击【新增测点】展开录入表单，必填项：
测点编码、测点名称、寄存器地址、数值类型、量程上下限、报警上下限、计量单位
- 保存：录入信息入库，自动刷新列表展示新增数据
- 取消：关闭弹窗，不保存

#### 4.1.3 编辑功能
选中已有测点，可修改全部寄存器参数，修改后保存更新数据库、实时刷新前端列表。

### 4.2 采集隔离规则（关键）
1. **新增寄存器默认【采集停用】**：仅做数据保存、页面展示，**不自动加入原有采集任务，不影响现有设备采集逻辑**；
2. 如需接入实时采集：手动勾选【采集启用】，勾选后系统将该寄存器纳入周期采集队列；
3. 报文筛选：表格复选框勾选=该寄存器数据参与文件/MQ报文生成，未勾选过滤剔除。

## 五、数据存储与页面展示规则
1. 保存：新增/编辑的寄存器参数、定时配置、项目勾选内容全部落地数据库；
2. 展示：所有已录入寄存器统一列表展示，用颜色/标签区分「已启用采集」「仅存档不采集」两种状态；
3. 报文生成：定时触发时，仅读取勾选寄存器数据，按MT/T1201.2-2023标准拼装报文。

## 六、报文生成沿用原有标准
1. 文件命名规则、消息队列名称不变；
2. 分隔符：字段`;`、单条结尾`~`、文件结尾`||`；
3. 故障缺省值统一：`-9999`；
4. 文件编码：UTF-8无BOM。

## 七、配置保存
全部配置完成后点击【保存配置】，系统永久存储整套配置，按配置自动定时生成对应报文。